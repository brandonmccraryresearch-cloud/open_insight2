# DO NOT commit this file with a real key.
# Set GEMINI_API_KEY in Vercel Project Settings → Environment Variables instead.
# For local dev only, copy .env.local.example → .env.local and add your key.
# This file is .gitignore'd but was accidentally committed — never add a real key here.
GEMINI_API_KEY=
